{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm install && npm run build && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
} 